package com.javaeight;

import java.util.Arrays;
import java.util.Iterator;
import java.util.List;

public class Stream1 {

	public static void main(String[] args) {

		List<String> name = Arrays.asList("Sakib", "Asad", "Arshad", "Aamir");

		// name.stream().filter(str -> str.endsWith("d")).forEach(System.out::println);

		// name.stream().filter(str1 ->
		// Character.isUpperCase(str1.charAt(2))).forEach(System.out::println);

		/*
		 * for (;; System.out.println("Hiiii")) {
		 * 
		 * 
		 * }
		 */
		
	}
}
